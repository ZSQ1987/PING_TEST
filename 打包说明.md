# PING测试工具打包说明

## 一、项目概述

这是一个基于PySide6开发的PING测试工具，支持批量IP测试、持续输出、域名支持等功能。本说明将指导您如何将该工具打包成可执行文件，以便在没有Python环境的机器上运行。

## 二、环境准备

### 1. 安装Python

确保您的系统已安装Python 3.8或更高版本。您可以从[Python官网](https://www.python.org/downloads/)下载并安装。

### 2. 安装依赖项

打开命令提示符（Windows）或终端（Linux/macOS），进入项目目录，执行以下命令安装依赖：

```bash
pip install pyside6
```

### 3. 安装打包工具

我们使用PyInstaller进行打包：

```bash
pip install pyinstaller
```

## 三、打包步骤

### 1. 清理项目目录

确保项目目录中只有必要的文件：
- `ping_test_2025.py` - 主程序文件
- `saved_ips.json` - 保存的IP地址文件（可选）

### 2. 执行打包命令

在项目目录中执行以下命令：

```bash
pyinstaller --onefile --windowed --name "PING测试工具" ping_test_2025.py
```

命令参数说明：
- `--onefile` - 生成单个可执行文件
- `--windowed` - 生成窗口应用程序，不显示命令行窗口
- `--name` - 指定生成的可执行文件名称

### 3. 查看打包结果

打包完成后，会在项目目录中生成以下文件和目录：
- `dist/` - 包含生成的可执行文件
- `build/` - 打包过程中生成的临时文件
- `PING测试工具.spec` - 打包配置文件

## 四、压缩打包结果

为了方便分发，您可以将可执行文件压缩成ZIP文件：

### 1. Windows系统

1. 打开`dist`目录
2. 右键点击生成的`PING测试工具.exe`文件
3. 选择"发送到" → "压缩(zipped)文件夹"
4. 命名为`PING测试工具.zip`

### 2. Linux/macOS系统

在终端中执行以下命令：

```bash
cd dist
zip -r "PING测试工具.zip" "PING测试工具"
```

## 五、使用说明

### 1. 解压

将压缩文件解压到任意目录。

### 2. 运行

双击解压后的可执行文件即可运行PING测试工具。

### 3. 注意事项

- 首次运行时，工具会在当前目录创建`saved_ips.json`文件，用于保存IP地址。
- 工具支持拖放操作，您可以将IP地址文件拖放到IP输入框中。
- 持续PING模式下，需要手动点击"停止测试"按钮或关闭输出窗口来停止测试。

## 六、故障排除

### 1. 打包失败

如果打包过程中出现错误，请检查：
- Python版本是否正确
- 依赖项是否已正确安装
- 项目文件是否完整

### 2. 运行失败

如果生成的可执行文件无法运行，请尝试：
- 检查系统是否满足运行要求
- 确保可执行文件所在目录有写入权限
- 查看是否有错误信息提示

### 3. 功能异常

如果某些功能无法正常使用，请检查：
- 网络连接是否正常
- 防火墙是否允许PING命令执行
- 输入的IP地址或域名是否正确

## 七、更新日志

- **v1.0** - 初始版本，支持基本的PING测试功能
- **v1.1** - 增加持续PING模式和实时输出窗口
- **v1.2** - 支持域名输入和IP地址保存功能
- **v1.3** - 优化GUI布局和测试结果显示

## 八、联系方式

如果您在使用过程中遇到问题，请联系开发者寻求帮助。